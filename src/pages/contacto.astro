---
// src/pages/contacto.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Contacto">
  <Header />
  
  <main class="min-h-screen bg-white">
    {/* Hero */}
    <section class="bg-gradient-to-b from-gray-50 to-white py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
          Contacto
        </h1>
        <p class="text-lg text-gray-600 max-w-2xl">
          Estamos aquí para ayudarte. Ponte en contacto con nosotros de la forma que prefieras.
        </p>
      </div>
    </section>

    {/* Contenido */}
    <section class="py-16">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid md:grid-cols-2 gap-12">
          
          {/* Información de contacto */}
          <div>
            <h2 class="text-3xl font-bold text-gray-900 mb-8">Datos de Contacto</h2>
            
            <div class="space-y-8">
              <div class="flex gap-4">
                <div class="flex-shrink-0">
                  <div class="w-12 h-12 bg-bergner-red/10 rounded-lg flex items-center justify-center">
                    <span class="text-2xl">📍</span>
                  </div>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Dirección</h3>
                  <p class="text-gray-600">
                    Av. Corrientes 1234<br/>
                    Buenos Aires, Argentina 1043
                  </p>
                </div>
              </div>

              <div class="flex gap-4">
                <div class="flex-shrink-0">
                  <div class="w-12 h-12 bg-bergner-red/10 rounded-lg flex items-center justify-center">
                    <span class="text-2xl">📞</span>
                  </div>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Teléfono</h3>
                  <p class="text-gray-600">+54 9 11 0000-0000</p>
                  <p class="text-gray-500 text-sm">Lunes a Viernes, 9:00 - 18:00</p>
                </div>
              </div>

              <div class="flex gap-4">
                <div class="flex-shrink-0">
                  <div class="w-12 h-12 bg-bergner-red/10 rounded-lg flex items-center justify-center">
                    <span class="text-2xl">📧</span>
                  </div>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">Email</h3>
                  <p class="text-gray-600">contacto@metalurgicapipo.com.ar</p>
                  <p class="text-gray-500 text-sm">Respuesta en 24 horas</p>
                </div>
              </div>

              <div class="flex gap-4">
                <div class="flex-shrink-0">
                  <div class="w-12 h-12 bg-green-600/10 rounded-lg flex items-center justify-center">
                    <span class="text-2xl">💬</span>
                  </div>
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 mb-1">WhatsApp</h3>
                  <p class="text-gray-600 mb-2">+54 9 11 0000-0000</p>
                  <a 
                    href="https://wa.me/5491100000000?text=Hola, quiero consultar..."
                    target="_blank"
                    rel="noopener noreferrer"
                    class="inline-block bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg font-medium transition-colors text-sm"
                  >
                    Abrir WhatsApp
                  </a>
                </div>
              </div>
            </div>

            {/* Redes Sociales */}
            <div class="mt-12 pt-8 border-t border-gray-200">
              <h3 class="font-bold text-gray-900 mb-4">Síguenos</h3>
              <div class="flex gap-4">
                <a href="#" class="w-12 h-12 bg-gray-100 hover:bg-bergner-red hover:text-white rounded-full flex items-center justify-center transition-all text-xl">
                  f
                </a>
                <a href="#" class="w-12 h-12 bg-gray-100 hover:bg-bergner-red hover:text-white rounded-full flex items-center justify-center transition-all text-xl">
                  📷
                </a>
                <a href="#" class="w-12 h-12 bg-gray-100 hover:bg-bergner-red hover:text-white rounded-full flex items-center justify-center transition-all text-xl">
                  🎥
                </a>
              </div>
            </div>
          </div>

          {/* Formulario de contacto */}
          <div>
            <h2 class="text-3xl font-bold text-gray-900 mb-8">Envíanos un Mensaje</h2>
            
            <form class="space-y-6" action="https://formspree.io/f/YOUR_FORM_ID" method="POST">
              <div>
                <label for="nombre" class="block text-sm font-medium text-gray-700 mb-2">
                  Nombre Completo
                </label>
                <input 
                  type="text" 
                  id="nombre" 
                  name="nombre"
                  required
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-bergner-red"
                  placeholder="Tu nombre"
                />
              </div>

              <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                  Email
                </label>
                <input 
                  type="email" 
                  id="email" 
                  name="email"
                  required
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-bergner-red"
                  placeholder="tu@email.com"
                />
              </div>

              <div>
                <label for="telefono" class="block text-sm font-medium text-gray-700 mb-2">
                  Teléfono
                </label>
                <input 
                  type="tel" 
                  id="telefono" 
                  name="telefono"
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-bergner-red"
                  placeholder="+54 9 11 0000-0000"
                />
              </div>

              <div>
                <label for="asunto" class="block text-sm font-medium text-gray-700 mb-2">
                  Asunto
                </label>
                <input 
                  type="text" 
                  id="asunto" 
                  name="asunto"
                  required
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-bergner-red"
                  placeholder="¿En qué podemos ayudarte?"
                />
              </div>

              <div>
                <label for="mensaje" class="block text-sm font-medium text-gray-700 mb-2">
                  Mensaje
                </label>
                <textarea 
                  id="mensaje" 
                  name="mensaje"
                  required
                  rows="5"
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-bergner-red resize-none"
                  placeholder="Cuéntanos más sobre tu consulta..."
                ></textarea>
              </div>

              <button 
                type="submit"
                class="w-full bg-bergner-red hover:bg-red-700 text-white py-3 px-6 rounded-lg font-bold transition-colors"
              >
                Enviar Mensaje
              </button>
            </form>

            <p class="text-sm text-gray-500 text-center mt-4">
              También puedes contactarnos directamente por WhatsApp para una respuesta más inmediata.
            </p>
          </div>

        </div>
      </div>
    </section>

    {/* Mapa */}
    <section class="py-16 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-gray-900 mb-8 text-center">Ubicación</h2>
        <div class="rounded-xl overflow-hidden shadow-lg h-96 bg-gray-200 flex items-center justify-center">
          <p class="text-gray-500 font-semibold">Mapa interactivo (integrar con Google Maps)</p>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>
