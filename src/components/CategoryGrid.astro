---
// src/components/CategoryGrid.astro

interface Category {
  id: string;
  name: string;
  icon: string;
  slug: string;
}

const currentPath = Astro.url.pathname;
const isEnglish = currentPath.startsWith('/en');
const langPrefix = isEnglish ? '/en' : '/es';

const categoriesES: Category[] = [
  { id: '1', name: 'Válvulas', icon: '⚙️', slug: 'valvulas' },
  { id: '2', name: 'Compresores', icon: '🔋', slug: 'compresores' },
  { id: '3', name: 'Repuestos', icon: '🔧', slug: 'repuestos' },
  { id: '4', name: 'Mecanizado', icon: '🏗️', slug: 'mecanizado' },
  { id: '5', name: 'Servicios', icon: '🛠️', slug: 'servicios' },
  { id: '6', name: 'Proyectos', icon: '📋', slug: 'proyectos' },
];

const categoriesEN: Category[] = [
  { id: '1', name: 'Valves', icon: '⚙️', slug: 'valves' },
  { id: '2', name: 'Compressors', icon: '🔋', slug: 'compressors' },
  { id: '3', name: 'Spare Parts', icon: '🔧', slug: 'spares' },
  { id: '4', name: 'Machining', icon: '🏗️', slug: 'machining' },
  { id: '5', name: 'Services', icon: '🛠️', slug: 'services' },
  { id: '6', name: 'Projects', icon: '📋', slug: 'projects' },
];

const categories = isEnglish ? categoriesEN : categoriesES;

const texts = {
  tag: isEnglish ? 'Explore by Category' : 'Explora por Categoría',
  title: isEnglish ? 'Industrial Solutions' : 'Soluciones Industriales',
  subtitle: isEnglish ? 'Top quality components and maintenance for the industry' : 'Componentes de máxima calidad y mantenimiento para la industria'
};
---

<section class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <div class="text-center mb-16">
      <span class="inline-block text-bergner-red font-semibold text-sm uppercase tracking-widest mb-4">
        {texts.tag}
      </span>
      <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
        {texts.title}
      </h2>
      <p class="text-gray-600 max-w-2xl mx-auto text-lg">
        {texts.subtitle}
      </p>
    </div>

    <div class="grid grid-cols-2 md:grid-cols-3 gap-6">
      {categories.map((category) => (
        <a 
          href={`${langPrefix}/productos?categoria=${category.slug}`}
          class="group relative bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl p-8 text-center hover:from-bergner-red/5 hover:to-bergner-red/10 transition-all duration-300 border border-gray-200 hover:border-bergner-red/30"
        >
          {/* Efecto de fondo */}
          <div class="absolute inset-0 rounded-2xl bg-white opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          
          <div class="relative z-10">
            <div class="text-5xl mb-4">{category.icon}</div>
            <h3 class="text-xl font-bold text-gray-900 group-hover:text-bergner-red transition-colors">
              {category.name}
            </h3>
            <p class="text-gray-500 text-sm mt-2 group-hover:text-gray-600">
              Ver productos
            </p>
          </div>
        </a>
      ))}
    </div>

  </div>
</section>
